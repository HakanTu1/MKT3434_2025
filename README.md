
### 🎯 Added New Features

*   Desired Loss Function options such as MSE, MAE, Huber, Cross-Entropy and Hinge are added to both regression and classification properly.
*   Model Trainin Logic is uptaded according to the selected loss function.
*   Logic implemented to apply selected method splitting data into train/test sets.
*   An input field added for custom probabilities for Bayesian priors.
*   Scikit-learn’s SimpleImputer is used for missing data handling.
*   Users can select methods for handling missing values: Mean Imputation, Interpolation, Forward/Backward Fill.
*   GaussianNB with configurable var_smoothing and prior probabilities (uniform vs. user-defined) is implemented.

### However,

*  After countless attempts, I still couldn’t make any classical ML tool run properly.

### But at least,

* I think the GUI I delivered matches the expected criteria.

---

